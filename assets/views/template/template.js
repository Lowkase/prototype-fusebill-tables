// --------------------------------------------------------------------------------
// CONTROLLER - TEMPLATE
//---------------------------------------------------------------------------------

function subheaderSetTitles(title, description) {
    $("#subheader-title").text(title);
	$("#subheader-description").text(description);
}